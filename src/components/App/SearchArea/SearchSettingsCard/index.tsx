import React from 'react';
import {FlexWrapper} from '../../../Shared/FlexWrapper';
import {Card} from '../../../Shared/Card';

/**
 * Компонента настроек поиска билетов
 */
export const SearchSettingsCard: React.FC = () => (
	<Card>
		<FlexWrapper flexDirection="column">
			Настройки
		</FlexWrapper>
	</Card>
);